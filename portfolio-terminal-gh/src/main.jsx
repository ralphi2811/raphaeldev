import React from 'react'
import ReactDOM from 'react-dom/client'
import TerminalPortfolio from './TerminalPortfolio.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TerminalPortfolio />
  </React.StrictMode>,
)
